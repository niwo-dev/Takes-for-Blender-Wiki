site_name: Takes for Blender — User Guide
site_description: >-
  Stage Management, Variant Switching, and Batch Rendering pipeline
  for Product Visualization in Blender 5.1+.
site_author: Nikolaj Woroschilow
site_url: https://niwo-dev.github.io/Takes-for-Blender-Wiki/wiki/


validation:
  nav:
    omitted_files: ignore

copyright: "&copy; 2026 Nikolaj Woroschilow"

# NOTE: `extra:` is defined once, at the bottom of this file. YAML keeps only the
# last definition of a duplicate key, so a second block here would silently win
# over it (or be silently overridden) — keep them merged in one place.

plugins:
  - search
  - macros:
      module_name: macros
      on_error_fail: true

# Keeps the "What's New" section (changelog + roadmap) out of the search
# index — those pages are generated, so front matter can't carry the flag.
hooks:
  - hooks/search_exclude.py
  # Resolves the latest available addon version into `extra.addon_version`
  # for the header's GitHub version chip.
  - hooks/addon_version.py

theme:
  name: material
  custom_dir: docs/overrides
  palette:
    # 1. Standard Dark Mode (Default)
    - scheme: slate
      primary: black
      accent: orange
      toggle:
        icon: material/brightness-4
        name: Switch to Light Pattern
    # 2. Standard Light Mode
    - scheme: default
      primary: black
      accent: orange
      toggle:
        icon: material/brightness-7
        name: Switch to Dark Pattern
  features:
    # No navigation.tabs: the drawer now carries the whole tree, so a top
    # row of the same section names was the menu twice. Dropping it also
    # means the drawer opens at the TOP of the nav rather than scoped to
    # the current section.
    # Ajax page loads: Material swaps content instead of reloading, so the
    # header, drawer and search survive navigation. Every custom script in
    # docs/javascripts/ subscribes to document$ rather than DOMContentLoaded,
    # because that fires once per session under instant loading.
    - navigation.instant
    - navigation.instant.progress
    - navigation.path
    # No navigation.sections / navigation.expand: those render groups as flat,
    # always-open headers. Without them the sidebar groups collapse, which is
    # what keeps a 17-page section from swamping the menu.
    - navigation.top
    # No navigation.indexes: it turns a section's own page into the section
    # link, so some menu rows navigated while others expanded. Every group
    # now does the same thing -- expand.
    - navigation.footer
    - navigation.tracking
    - navigation.tooltip
    - search.suggest
    - search.highlight
    - content.code.copy
    - content.tabs.link
    - content.tooltips
    - toc.follow
  icon:
    repo: fontawesome/brands/github
    logo: material/layers-triple
  font:
    text: Inter
    code: JetBrains Mono

nav:
  - Home: index.md

  # Sections are named for what the reader wants, not for how the addon is built:
  # learn / do / look up / understand. Page PATHS are deliberately unchanged --
  # the addon deep-links ~867 controls at page#anchor targets, so moving files
  # would break right-click "Online Manual" for no reader benefit. Nav titles are
  # independent of paths, which is what makes this regrouping free.
  - Start Here:
    - Install Takes: getting_started/installation.md
    - Your First Steps: getting_started/first_steps.md
    - Your First Take: getting_started/first_take.md
    - Your First Batch Render: getting_started/first_batch_render.md

  - Guides:
    - Working with Takes:
      - Switch Between Takes: workflows/switch_takes.md
      - Keep an Object's Own Animation: workflows/keep_an_objects_own_animation.md
      - Lock a Value: workflows/lock_a_value.md
    - Looks & Variants:
      - Make Material Variants: workflows/material_variants.md
      - Make Animation Variants: workflows/animation_variants.md
      - Tag & Filter Takes: workflows/tag_and_filter.md
    - Rendering:
      - Batch Render Your Takes: workflows/batch_rendering.md
      - Name Your Output Files: workflows/name_output_files.md
      - Use Render Presets: workflows/use_render_presets.md
    - Getting Around:
      - Bookmark a Property: workflows/bookmark_a_property.md

  - How It Works:
    - The Takes System: features/takes.md
    - Takes & the Review Loop: features/vl_versions.md
    - The Cascade: features/cascade.md
    - Rest State: features/rest_state.md
    - Takes vs Other Tools: comparisons.md

  - Buttons & Settings:
    - Panels:
      - Navigation Panel: interface/navigation_panel.md
      - Inspector Panel: interface/inspector_panel.md
      - Context Properties: interface/context_properties.md
      - Variant Tree: interface/variant_tree.md
      - Globals Panel: features/globals.md
      - Keyboard Shortcuts: interface/hotkeys.md
    - Features:
      - Setting Up Looks:
        - Variant Switch: features/variant_switch.md
        - Render Presets: features/render_presets.md
        - Tag Library: features/tags.md
        - Rules: features/rules.md
      - Naming & Output:
        - Smart Output: features/smart_output.md
        - Custom Tokens: features/custom_tokens.md
        - Batch Render: features/batch_render.md
      - Working Faster:
        - Pie Menus: features/pie_menus.md
        - Bookmarks: features/bookmarks.md
        - View Layer Preview: features/vl_preview.md
        - Viewport Sync: features/viewport_sync.md
      - Modes & Safety:
        - Still Mode: features/still_mode.md
        - Value Lock: features/value_lock.md
      - Advanced:
        - Video Sequencer: features/sequencer.md
        - Process Monitor: features/process_monitor.md
        - AI Assistant: features/ai_assistant.md
    - Preferences:
      - Overview: preferences/index.md
      - Workflow: preferences/workflow.md
      - Interface: preferences/ui.md
      - Data: preferences/data.md
      - Developer: preferences/debug.md
      - Advanced: preferences/advanced.md

  - FAQ: faq.md

  - What's New:
    - Overview: whats_new/index.md
    - Changelog: changelog/index.md
    - Changelog (Dev): changelog/dev.md
    - Roadmap: roadmap/index.md

markdown_extensions:
  - admonition
  - pymdownx.details
  - pymdownx.superfences:
      custom_fences:
        - name: mermaid
          class: mermaid
          format: !!python/name:pymdownx.superfences.fence_code_format
  - pymdownx.tabbed:
      alternate_style: true
  - pymdownx.highlight:
      anchor_linenums: true
  - pymdownx.inlinehilite
  - pymdownx.snippets
  - pymdownx.emoji:
      emoji_index: !!python/name:material.extensions.emoji.twemoji
      emoji_generator: !!python/name:material.extensions.emoji.to_svg
  - pymdownx.keys
  - pymdownx.tasklist:
      custom_checkbox: true
  - attr_list
  - md_in_html
  - def_list
  - tables
  - toc:
      permalink: true
      # The right-hand contents column shows only top-level headings — e.g. the
      # changelog lists versions (##), not each Added/Changed/Fixed (###).
      toc_depth: 2

extra_css:
  - stylesheets/extra.css?v=ba1fc960

extra_javascript:
  - javascripts/nav-icons.js?v=b4328d39
  - javascripts/toc-icons.js?v=e5d33e6b
  - javascripts/roadmap-sort.js?v=71afec24
  - javascripts/changelog-toc.js?v=9b4e6d01
  - javascripts/toc-popover.js?v=16b0ee56

# No `social:` list. It only ever held the GitHub icon, and the header already
# carries a GitHub link with the version chip — the footer copy was the same
# destination a second time. Adding a key here brings the footer icon row back.
extra: {}
