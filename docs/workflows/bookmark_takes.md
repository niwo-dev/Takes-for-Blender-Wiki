---
title: Bookmark a Property
search:
  exclude: true
hide:
  - toc
---

<!-- Forwarding stub. This page was renamed: bookmarks point at animatable
     PROPERTIES, not at takes, so "Bookmark Takes" described something the
     feature does not do. The file stays behind so a saved link still lands
     somewhere useful instead of on a 404.

     A meta refresh rather than a plugin: the wiki has no redirect plugin, and
     adding one to a CI whose install list lives elsewhere is a bigger change
     than this page is worth. If more renames pile up, that trade flips —
     switch to mkdocs-redirects then and delete this file. -->

<meta http-equiv="refresh" content="0; url=../bookmark_a_property/">

# Page moved

This page is now [**Bookmark a Property**](bookmark_a_property.md).

Your browser should take you there on its own. If it does not, use the link above.
